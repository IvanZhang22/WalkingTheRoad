# Changelog

## [3.1.1] - 2026-08-18

### Added
- 同一清小搭 `sessionId` 内的轻量多项目工作区：支持新建、切换、归档、恢复和 30 天归档清理。
- 统一 `project_context`：保存研究主题、研究问题、对象、材料、人工确认结果、四阶段状态和当前交互菜单。
- `active_menu` 菜单状态，数字只在当前菜单内解释；用户明确的自然语言请求优先于旧菜单。

### Changed
- 主对话改为“回应意图 → 给出下一步”的研究助理语气，并在项目主页显示已知信息和进度。
- 上下文发生冲突时先请求研究者确认，避免静默覆盖既有项目记录。

所有重要变更按版本记录。本项目在当前阶段使用三段式版本号。

## [Unreleased]

## [3.1.0] - 2026-08-17

### Added

- 合入 PR #11 的多模态基础能力：安全文档解析、音频转写、图片与扫描 PDF OCR、置信度/定位门控及 W3 证据链。
- 保留 v3.0.0 的清小搭会话旅程、隐私授权、材料确认、人工确认和项目进度状态。

### Deployment note

- Vercel Blob 大文件直传仅用于 Vercel Preview；腾讯云正式环境暂不启用，待替换为腾讯云 COS 预签名上传并完成真实服务验收。

## [3.0.0] - 2026-08-16

### Added

- 清小搭正式研究旅程：可解释且可人工纠正的任务推荐、逐步输入、返回与结束导航。
- 会话级项目进度：W1—W4 分别记录进行中、AI 已生成与研究者确认状态；只有确认后的成果进入可复制工作包。
- 材料处理守门：上传授权、匿名化提示、材料确认、低置信片段不静默进入证据链。
- 研究诚信与方法边界：拒绝虚构材料，拒绝由少量质性材料推断总体比例。
- 三段式失败提示，明确说明发生了什么、是否影响已有结果及下一步操作。

## [2.3.0] - 2026-08-16

### Added

- 清小搭会话编排层：以平台 `sessionId` 保存菜单选择与逐步输入，服务重启后仍可恢复同一会话。
- 用户可回复 1–4 选择研究设计、访谈设计、材料分析或结论质检；支持“主菜单”“上一步”“结束”。
- 已收集完成的输入直接调用既有四工作流执行与证据核验逻辑，不再向用户暴露内部工作流编号、协议模式或不存在的表单。
- 无 `sessionId` 的一次性标准 OpenAI 调用保留 v2.2 的附件即分析兼容行为。

## [2.2.0] - 2026-08-08

### Added

- v2.2 多模态 `text`、`input_audio`、`file` 消息契约；
- 厂商无关的 ASR、OCR、文档解析接口和统一材料/片段模型；
- 无密钥 Mock Provider、多附件故障隔离及低置信/定位缺失门控；
- 同一 `/v1/chat/completions` 的多模态预检摘要，不回显附件 URL 或材料全文。
- 异步安全下载器，逐跳检查 DNS/重定向/实际连接地址、大小、MIME 和文件头；
- TXT、Markdown、DOCX、PDF 文字层真实解析，以及字符区间和 PDF 页码定位。
- 阶跃异步文件 ASR、分句毫秒时间戳、轮询节流和失败关闭；
- Deepgram Nova-3 备用 ASR，提供分句时间戳、置信度和说话人；
- 百度图片/扫描 PDF OCR，提供页码、bbox 和行置信度；
- 自动可用片段直连 W3 证据提取、后端精确引文核验和主题生成；
- 同一 Chat Completion 端点的无费用端到端多模态验收测试。

### Security

- 拒绝 data URL、本地路径、显式内网/元数据 IP、URL 凭据和路径穿越文件名；
- Live 环境没有真实 Provider 时失败关闭，不使用 Mock 内容冒充真实识别。
- 临时附件在单次处理后强制删除，来源 URL 和临时路径不进入公开材料对象；
- 下载客户端不读取系统代理环境变量，扫描 PDF 不会以空文本进入证据分析。
- 低置信、缺置信度或缺定位的片段不会进入 W3 提示词；跨片段引文和错误片段编号由后端拒绝；
- Deepgram、阶跃、百度密钥与原始附件 URL 不进入公开材料对象或响应。

### Known limitations

- 阶跃文件 ASR 官方不返回置信度，结果需人工复核；高置信自动闭环需选择 Deepgram；
- 百度 OCR 真实使用前需另行开通服务并在部署环境配置 AK/SK；
- 境外 Vercel 到清小搭的既有网络限制没有在本版解决，国内部署顺延至 v2.3；
- 视频不在 v2.2 范围内。

## [2.1.0] - 2026-08-05

### Added

- Vercel Preview 部署、环境变量和公开健康检查；
- 线上 `GET /v1/models`、Bearer 鉴权和真实 `POST /v1/chat/completions` 验证；
- 显式 UTF-8 JSON 响应，修复 OpenAI 兼容入口中文输出；
- 国内部署与清小搭接入失败的可复现记录。

### Known limitations

- 清小搭服务端无法访问境外 Vercel 地址，连通性检测返回 `SocketException`；
- 本版只完成境外预览部署，不宣称已经接入或上线清小搭；
- 标准 Chat 入口当前只完成意图识别和工作流推荐，尚未开放多模态输入。

## [2.0.0] - 2026-08-05

### Added

- OpenAI 兼容的 `GET /v1/models` 与 `POST /v1/chat/completions`；
- 独立 `AGENT_API_KEY` Bearer 鉴权，和模型供应商密钥完全隔离；
- 标准非流式响应、SSE role/content/stop/[DONE] 帧与最小探测响应；
- 协议契约测试，以及 Vercel/清小搭部署与验收说明；
- Vercel Python Function 入口与 120 秒函数配置。

### Unchanged

- 四工作流、项目卡、模型提示词和业务输出未改动；
- 尚未部署到 Vercel、接入清小搭或开放文件/音频输入。

## [1.4.0] - 2026-08-05

### Added

- GitHub Actions CI：仓库安全、Ruff、Mypy、Pytest、Node项目卡测试和三组Mock回归；
- 标签发布工作流：版本一致性检查、安全源码包、SHA-256和GitHub Release；
- Pull Request、Bug和功能建议模板；
- 仓库安全检查、统一质量门和发布包构建脚本；
- 团队协作、安全、首次建库、发布和回滚说明。

### Changed

- 版本统一升级到 v1.4.0；
- `.gitignore` 扩大到本地数据、运行结果、构建产物和编辑器配置；
- 工程目录作为独立 Git 仓库根目录，不包含赛事说明、团队 Word 或根目录其他资料。

### Unchanged

- W1—W4业务逻辑、v1.2意图路由和v1.3浏览器项目卡保持不变；
- 不增加知识库、云部署、OpenAI兼容协议或多模态能力。

## [1.3.0] - 2026-08-05

- 增加浏览器项目卡、结构化人工写回、四工作流串联、修订记录和过期状态。

## [1.2.0] - 2026-08-05

- 增加自由描述意图识别、工作流推荐和人工纠正。

## [1.1.0] - 2026-08-04

- 完成四工作流本地全代码迁移、文档解析、结构校验和W3/W4确定性引文核验。
