# v3.3.1 音频转写人工核对

阶跃文件 ASR 可返回带时间戳的转写，但不一定带分句置信度。v3.3.1 不再把这种结果误报为“无法提取文本”：系统先展示转写预览，只有研究者对照原音频明确确认后，才将其送入 W3 证据分析。

部署后用同一会话进入 W3、完成材料授权、上传匿名 mp3/wav，再依次选择“分类正确，开始分析”与“已核对转写，继续”。查看 `journalctl -u xingxiaodao -n 200 --no-pager` 可获得不含材料正文、URL 或密钥的结构化处理日志。
