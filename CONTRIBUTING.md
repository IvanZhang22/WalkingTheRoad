# 行小道协作指南

## 一、职责与评审顺序

1. 马俊博：产品设计、整体定调和社科学术口径；审核产品定位、Prompt原则和赛事表达。
2. 岳承鑫：资料收集整理和测试材料；提交来源、适用范围、限制与脱敏说明。
3. 马仲琪：多模态技术负责人；以独立分支和可复现测试交付音频、文件解析等模块。
4. 张一凡：技术架构负责人；维护接口、项目卡、质量门、版本号、Release和最终合并。

成员投入有限时，核心工程不等待口头承诺；未完成事项保留为 Issue，不在发布说明中写成已有能力。

## 二、分支与提交

- `main`：始终保持可启动、可测试、可回滚；禁止直接推送功能改动。
- `feat/<简短主题>`：新功能。
- `fix/<简短主题>`：缺陷修复。
- `docs/<简短主题>`：文档和资料。
- `chore/<简短主题>`：测试、CI和工程维护。

每个分支只解决一个明确问题。提交信息使用简短中文或英文，例如：

```text
feat: 增加音频解析接口适配层
fix: 修复项目卡导入后的阶段状态
docs: 补充W3学术规则来源
```

## 三、Pull Request

1. 从最新 `main` 新建分支；
2. 不提交 `.env`、密钥、真实姓名、联系方式或未脱敏材料；
3. 本地运行 `python scripts/run_quality_gate.py`；
4. 填完 PR 模板，明确改了什么、没改什么、测试结果和回滚方法；
5. 等待 GitHub Actions 通过；
6. 至少由一名非提交者评审，涉及产品或学术口径时由马俊博定调，涉及主架构时由张一凡验收；
7. 使用 squash merge，PR标题成为 `main` 上的提交说明。

## 四、资料与多模态交付

资料不得直接堆入工程根目录。先在 Issue 说明来源、许可证或使用权限、适用工作流、脱敏状态、限制和预期验收，再决定是否进入独立知识包或测试夹具。

马仲琪的模块必须提供：输入输出契约、依赖和环境变量、成功/失败样例、隐私边界、测试命令以及与统一 Agent 入口的建议整合点。不得携带真实 Token 或只能在个人电脑复现的绝对路径。

## 五、版本与回滚

- 功能阶段使用 `v1.4.0` 这类三段式标签；
- 修复已发布版本只增加补丁号，例如 `v1.4.1`；
- 不移动或覆盖已经推送的标签；
- 严重问题从上一个稳定标签建立修复分支，重新通过质量门后发布；
- 项目卡 Schema、公开接口或安全边界变化必须写入 CHANGELOG。
